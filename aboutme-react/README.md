About Me React section

Run locally:

```bash
cd aboutme-react
npm install
npm run dev
```

This small Vite + React app contains reusable components for a dark, neon-accented About Me section designed for integration into a portfolio.